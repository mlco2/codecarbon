[tox]
envlist = py37

[testenv:api]
deps =
    pytest
    -rrequirements-test.txt

commands =
    pip install -e .
    python -m pytest -vv


recreate =
    false

[pytest]
testpaths =
    tests/infra
