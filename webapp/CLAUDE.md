# Webapp

Read [AGENTS.md](./AGENTS.md) in this folder before doing anything here, and
treat it as binding for all work under `webapp/` — it holds both the working
agreement (propose before editing, update tests and docs, never commit) and the
webapp's own conventions.

This file is gitignored; `AGENTS.md` is the checked-in source of truth.
